using UnityEngine;
using UnityEditor;

//[CustomEditor(typeof(CircularPlaneGenerator))]
public class CircularPlaneGeneratorEditor /*: Editor*/
{
   /* public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        CircularPlaneGenerator generator = (CircularPlaneGenerator)target;

        GUILayout.Space(10);

        if (GUILayout.Button("Generate Circular Plane with Collider")) // Adjust the button label
        {
            generator.GenerateCircularMesh(); // Call the new method
        }
    }*/
}
